# Lakers Python
Python wrapper for the `lakers` crate.

To build and test:
```bash
maturin develop
pytest
```

# Requirements

```
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip maturin pytest
pip freeze
```
